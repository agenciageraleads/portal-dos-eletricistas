# Portal do Eletricista - Web App 📱

Frontend PWA (Progressive Web App) desenvolvido em **Next.js**.

## Tecnologias Key
*   **Framework:** Next.js 14 (App Router).
*   **Styling:** TailwindCSS + Shadcn/ui (para componentes base).
*   **State:** React Context / Zustand.
*   **Icons:** Lucide React.

## Estrutura de Pastas
*   `app/`: Rotas/Páginas (App Router).
*   `components/ui`: Componentes base reutilizáveis (Botões, Inputs).
*   `components/features`: Componentes de negócio (CatalogGrid, BudgetCart).
*   `lib/`: Utils e configurações.

## Setup Específico

```bash
# Instalar deps
npm install

# Rodar localmente
npm run dev
```

Acesse [http://localhost:3000](http://localhost:3000).
